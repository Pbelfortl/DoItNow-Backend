export type user = {
    name: string,
    email: string,
    password: string,
    idade: number,
    altura: number,
    peso: number
}

export type account = {
    email: string,
    password: string
}